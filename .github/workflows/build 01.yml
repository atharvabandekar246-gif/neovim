name: Build Pipeline

on:
  push:
    branches:
      - 'SCRUM-8-atharva-bandekar-integrate-npm-project-with-dev-ops-integration'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Display Build Message
        run: echo "GitHub Actions Build Successful"
